global class WebServiceNotes2 {
/*  
    //Custom webservice for SOAP
    webservice static Id makeContact(String contactLastName, Account a){
        Contact = new Contact(lastname = contactLastName, AccountId = a.Id);
        insert c;
        return c.Id;
    }
*/
}